import { ConfigFile } from '../../runtime';
export default function preprocessorTest(content: string, { route, ...cfg }?: {
    route: boolean;
} & Partial<ConfigFile>): Promise<import("svelte/types/compiler/interfaces").Ast>;
declare type Partial<T> = {
    [P in keyof T]?: T[P];
};
export {};
