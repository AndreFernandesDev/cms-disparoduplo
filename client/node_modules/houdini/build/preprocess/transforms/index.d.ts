import { Config, Transform } from '../../common';
import * as types from '../types';
export default function applyTransforms(config: Config, doc: {
    content: string;
    filename: string;
}, pipeline?: Transform<types.TransformDocument>[]): Promise<{
    code: string;
    dependencies: string[];
}>;
