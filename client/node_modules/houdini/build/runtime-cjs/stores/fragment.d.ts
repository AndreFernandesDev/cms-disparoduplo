import { ConfigFile, FragmentStore, GraphQLObject, QueryArtifact } from '../lib';
import { PaginatedHandlers } from '../lib/pagination';
export declare function fragmentStore<_Data extends GraphQLObject, _Input = {}>({ artifact, config, paginatedArtifact, paginationMethods, }: {
    artifact: QueryArtifact;
    config: ConfigFile;
    paginated: QueryArtifact;
    paginatedArtifact?: QueryArtifact;
    paginationMethods: {
        [key: string]: keyof PaginatedHandlers<_Data, _Input>;
    };
}): FragmentStore<_Data | null>;
