import type { Maybe, Script } from './types';
export declare type ParsedSvelteFile = {
    instance: Maybe<Script>;
    module: Maybe<Script>;
};
export declare function parseFile(str: string): Promise<ParsedSvelteFile>;
export declare function findScriptInnerBounds({ start, end, text, }: {
    start: number;
    end: number;
    text: string;
}): [number, number];
