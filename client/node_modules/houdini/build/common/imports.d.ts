import { Statement } from '@babel/types';
import { Config } from '.';
export declare function ensureStoreImport({ config, artifact, body, local, }: {
    config: Config;
    artifact: {
        name: string;
    };
    body: Statement[];
    local?: string;
}): string;
export declare function ensureStoreFactoryImport({ config, artifact, body, }: {
    config: Config;
    artifact: {
        name: string;
    };
    body: Statement[];
}): string[];
export declare function ensureArtifactImport({ config, artifact, body, local, }: {
    config: Config;
    artifact: {
        name: string;
    };
    body: Statement[];
    local?: string;
}): string;
export declare function ensureImports<_Count extends string[] | string>({ config, body, import: importID, sourceModule, importKind, }: {
    config: Config;
    body: Statement[];
    import: _Count;
    sourceModule: string;
    importKind?: 'value' | 'type';
}): _Count;
