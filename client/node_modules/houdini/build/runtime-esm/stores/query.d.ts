import { GraphQLObject, QueryStore } from '..';
import type { ConfigFile, QueryArtifact } from '../lib';
import { PaginatedHandlers } from '../lib/pagination';
export declare function queryStore<_Data extends GraphQLObject, _Input>({ config, artifact, storeName, paginationMethods, paginated, }: {
    config: ConfigFile;
    artifact: QueryArtifact;
    paginated: boolean;
    storeName: string;
    paginationMethods: {
        [key: string]: keyof PaginatedHandlers<_Data, _Input>;
    };
}): QueryStore<_Data, _Input>;
