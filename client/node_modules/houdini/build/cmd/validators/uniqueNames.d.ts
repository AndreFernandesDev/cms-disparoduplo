import { Config } from '../../common';
import { CollectedGraphQLDocument } from '../types';
export default function uniqueDocumentNames(config: Config, docs: CollectedGraphQLDocument[]): Promise<void>;
