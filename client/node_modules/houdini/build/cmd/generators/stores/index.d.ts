import { Config } from '../../../common';
import { CollectedGraphQLDocument } from '../../types';
export default function storesGenerator(config: Config, docs: CollectedGraphQLDocument[]): Promise<void>;
