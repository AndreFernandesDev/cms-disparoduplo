import { Config } from '../../../common';
import { CollectedGraphQLDocument } from '../../types';
export declare function generateSubscriptionStore(config: Config, doc: CollectedGraphQLDocument): Promise<string>;
