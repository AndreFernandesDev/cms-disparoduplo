import { Config } from '../../../common';
import { CollectedGraphQLDocument } from '../../types';
export declare function generateIndividualStoreMutation(config: Config, doc: CollectedGraphQLDocument): Promise<string>;
