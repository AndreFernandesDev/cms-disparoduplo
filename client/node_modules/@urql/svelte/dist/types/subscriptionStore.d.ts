import type { DocumentNode } from 'graphql';
import { Client, OperationContext, TypedDocumentNode } from '@urql/core';
import { OperationResultStore, Pausable } from './common';
export interface SubscriptionArgs<Data = any, Variables = object> {
    client: Client;
    query: string | DocumentNode | TypedDocumentNode<Data, Variables>;
    variables?: Variables;
    context?: Partial<OperationContext>;
    pause?: boolean;
}
export declare function subscriptionStore<Data, Variables extends object = {}>(args: SubscriptionArgs<Data, Variables>): OperationResultStore<Data, Variables> & Pausable;
