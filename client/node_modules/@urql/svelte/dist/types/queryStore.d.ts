import type { DocumentNode } from 'graphql';
import { Client, OperationContext, TypedDocumentNode, RequestPolicy } from '@urql/core';
import { OperationResultStore, Pausable } from './common';
export interface QueryArgs<Data = any, Variables = object> {
    client: Client;
    query: string | DocumentNode | TypedDocumentNode<Data, Variables>;
    variables?: Variables;
    context?: Partial<OperationContext>;
    requestPolicy?: RequestPolicy;
    pause?: boolean;
}
export declare function queryStore<Data = any, Variables = object>(args: QueryArgs<Data, Variables>): OperationResultStore<Data, Variables> & Pausable;
